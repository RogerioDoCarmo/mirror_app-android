import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Button, ScrollView, StyleSheet, View } from 'react-native';
import { styled } from '@storybook/react-native-theming';
import Inspect from './Inspect';
const CountText = styled.Text(({ theme }) => ({
    color: theme.color.defaultText,
}));
export const ActionLogger = ({ actions, onClear }) => (_jsxs(ScrollView, { contentContainerStyle: { padding: 10 }, children: [_jsx(ScrollView, { horizontal: true, children: _jsx(View, { children: actions.map((action) => (_jsxs(View, { style: styles.row, children: [_jsx(View, { children: action.count > 1 ? _jsx(CountText, { children: action.count }) : null }), _jsx(View, { style: styles.grow, children: _jsx(Inspect, { name: action.data.name, value: action.data.args || action.data }) })] }, action.id))) }) }), _jsx(View, { children: _jsx(Button, { onPress: onClear, title: "CLEAR" }) })] }));
export default ActionLogger;
const styles = StyleSheet.create({
    grow: { flexGrow: 1 },
    row: { flexDirection: 'row' },
});
