import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from 'react';
import { Input } from './common';
import { useResyncValue } from './useResyncValue';
const TextType = ({ arg, onChange, isPristine }) => {
    const { setCurrentValue, key } = useResyncValue(arg.value, isPristine);
    const [focused, setFocused] = useState(false);
    return (_jsx(Input, { testID: arg.name, defaultValue: arg.value, onChangeText: (text) => {
            onChange(text);
            setCurrentValue(text);
        }, autoCapitalize: "none", underlineColorAndroid: "transparent", onFocus: () => setFocused(true), onBlur: () => setFocused(false), focused: focused }, key));
};
export default TextType;
