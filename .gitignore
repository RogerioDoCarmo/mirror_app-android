# This repo intentionally tracks android/ and node_modules/ — they're the
# whole point (a self-contained, buildable Gradle snapshot). Only ignore
# actual build output and local machine state below.

android/build/
android/app/build/
android/.gradle/
android/.cxx/
android/local.properties
.DS_Store
